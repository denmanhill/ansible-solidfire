---
- hosts: SolidfireNodes[0]
  connection: local
  tasks:
  - set_fact: 
      fact_for_SolidfireNodes: "NAA1"
  - name: First write debug
    debug:
      msg: hostvars['SolidfireNodes']

- hosts: ESXiHosts[0]
  connection: local
  tasks:

  - name: Second Write debug
    debug:
      msg: "{{ hostvars[groups['SolidfireNodes'][0]]['fact_for_SolidfireNodes'] }}"
